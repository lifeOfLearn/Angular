# Editor configuration, see https://editorconfig.org

#判斷是否為根目錄配置 如果此false,會一直往上找到root = true的跟目錄配置
root = true


#charset編碼
#indent_style:定義縮排(ex: space,tab)
#indent_size:縮排長度(ex: 2,4)
#insert_final_newline:文件最後是否有換行符號
#trim_trailing_whitespace:是否移除行尾空白

#quote_type:設定使用 ' 或 " 來包住字串
#ij_typescript_use_double_quotes:針對部分IDE設定
#max_line_length:每行最大長度

#全域配置,對所有都有效
[*]
charset = utf-8
indent_style = space
indent_size = 2
insert_final_newline = true
trim_trailing_whitespace = true

#對特定檔案生效
[*.ts]
quote_type = single
ij_typescript_use_double_quotes = false

[*.md]
max_line_length = off
trim_trailing_whitespace = false
